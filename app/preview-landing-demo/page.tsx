// TEMPORARY preview-only route for the click-gated landing demo concept —
// not linked from anywhere in the app, not meant to ship. Remove once the
// idea has been reviewed.
import { LandingDemo } from '@/components/landing-demo'

export default function PreviewLandingDemoPage() {
  return <LandingDemo />
}
